<h1>New Trader</h1>

<%= form_for [:admin, @trader], url:  admin_users_path do |f| %>
 <% if @trader.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trader.errors.count, "error") %> prohibited this object from being saved:</h2>
      <ul>
        <% @trader.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<%= form_with(model: @trader, url: admin_users_path, method: :post) do |form| %>
  <div class="field">
    <%= form.label :email %><br />
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :name %><br />
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit "Invite Trader" %>

  </div>
<% end %>
