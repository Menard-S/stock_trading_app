require 'rails_helper'

RSpec.describe Transaction, type: :model do
  # pending "add some examples to (or delete) #{__FILE__}"

  # describe "transactions" do
  #   let(:user) { FactoryBot.create(:user) } # Assuming you're using FactoryBot for creating test data

  #   it "returns the transactions associated with the user" do
  #     transaction = FactoryBot.create(:transaction, user: user) # Create a transaction associated with the user
  #     expect(user.transactions).to include(transaction)
  #   end

  #   it "does not return transactions associated with other users" do
  #     other_user = FactoryBot.create(:user)
  #     other_transaction = FactoryBot.create(:transaction, user: other_user)
  #     expect(user.transactions).not_to include(other_transaction)
  #   end
  # end
end
